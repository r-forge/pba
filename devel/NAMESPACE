useDynLib(pba, .registration = TRUE)

export(expit, logit, pba, pbaBackCalculate, pbaBiasCor, pbaBiasCorCheck,
	   pbaDistr, pbaIterate, pbaMisclassification, pbaMisclassificationCheck,
	   pbaPlotBias, pbaPlotEstimates, pbaSummary, pbaVariable, print.pba)	

import(plyr)
import(ggplot2)
import(QRMlib)